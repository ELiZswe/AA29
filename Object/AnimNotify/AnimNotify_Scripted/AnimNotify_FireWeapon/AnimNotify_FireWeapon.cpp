// All the original content belonged to the US Army


#include "AA29/Object/AnimNotify/AnimNotify_Scripted/AnimNotify_FireWeapon/AnimNotify_FireWeapon.h"

UAnimNotify_FireWeapon::UAnimNotify_FireWeapon()
{

}

void UAnimNotify_FireWeapon::Notify(AActor* Owner)
{
	/*
	Pawn(Owner).bIgnorePlayFiring = true;
	WeaponAttachment(Pawn(Owner).Weapon.ThirdPersonActor).ThirdPersonEffects();
	if (Pawn(Owner).Weapon.FireSound != None)
	{
		Pawn(Owner).Weapon.PlaySound(Pawn(Owner).Weapon.FireSound, 0, 1);
	}
	*/
}